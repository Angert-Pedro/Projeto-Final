import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    margin: 16,
    height: "100%",
    width: "90%",
  }, innerText: {
    fontSize: 16,
    fontWeight: "500",
  }, title: { 
    fontSize: 24, 
    fontWeight: "bold", 
    marginTop: 12, 
    marginLeft: 15,
  }, subTitle: { 
    fontSize: 18, 
    marginTop: 12, 
    fontWeight: "bold" 
  }, innerContainer: {
    marginTop: 10,
    marginBottom: 10,
  },
});

export default styles;
